# Based on AUR claude-code by Christopher Cooper, Jérôme Poulin, and Fabio Fontana
# Auto-generated from template - do not edit directly

pkgname=claude-code
pkgver=%%VERSION%%
pkgrel=1
pkgdesc="An agentic coding tool that lives in your terminal"
arch=('x86_64')
url="https://github.com/anthropics/claude-code"
license=('LicenseRef-claude-code')
depends=('bash')
# Binary is a self-contained Bun executable with embedded JS/resources - stripping breaks it
options=('!strip')
optdepends=(
	'git: allow Claude to use git'
	'github-cli: interact with GitHub'
	'glab: interact with GitLab'
	'ripgrep: enhanced file search'
	'tmux: agent team split panes'
	'bubblewrap: sandboxing'
	'socat: sandboxing'
)

source=("cc-legal::https://code.claude.com/docs/en/legal-and-compliance.md"
        "claude-${pkgver}-x86_64"
        "claude-wrapper.sh")
sha256sums=('SKIP'
            '%%SHA256%%'
            'SKIP')
noextract=("claude-${pkgver}-x86_64")

package() {
	install -Dm755 "${srcdir}/claude-${pkgver}-x86_64" "${pkgdir}/opt/claude-code/bin/claude"
	install -Dm755 "${srcdir}/claude-wrapper.sh" "${pkgdir}/usr/bin/claude"
	install -Dm644 "${srcdir}/cc-legal" "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
}
